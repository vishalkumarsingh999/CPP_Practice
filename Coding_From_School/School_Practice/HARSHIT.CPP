#include<stdio.h>
#include<conio.h>
#include<graphics.h>
#include<dos.h>
#include<snap.h>
void main()
{
int gdriver=DETECT,gmode;
int X,Y,I;
    inttgraph(&gdriver,&gmod,"(:\\TC\\BGE");
    X=getmaxx()/2;
    Y=getmax()/2;
    For(I=30;I<200;i++)
}
      delay(100);
      setcolor(i/10);
      arc (X,Y,0,180,i-10);
{
{clrscr()
getch()
}