
#include<iostream.h>
#include<conio.h>
void main()
  {   clrscr()
   int main() m=7;
    int N=[];{10,20,30,40};
    { cout<<"enter elements of array";
    cout<<"find element in array";
    cin>>"find";
   }


    for(i=1;i<m &&(N=[i]!=7);i++);
	continue;
    if
      cout<<"find not found";
      else
	  cout<<"find at location"<<i;

	  cout<<"endl";
	  }
