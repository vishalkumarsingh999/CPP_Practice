#include<iostream.h>
#include<fstream.h>
#include<stdlib.h>
int main()
{ system("cls");
ofstream fo",ios::in);
fin.seekg(0);
for(i=33;i<128;i++)
{    fin.get(ch);
     cout<<" "<<i<<"=";
     cout.put((char)(i));
     if(!(i%8))
	 {cout<<endl;line++;}
	 if(line>22){system("PAUSE");line=0;}
	 }
	 return 0;
 }
